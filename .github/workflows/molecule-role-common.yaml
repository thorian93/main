# https://github.com/ansible-collections/collection_template/blob/main/.github/workflows/ansible-test.yml

permissions:
  contents: read

name: Molecule Test Common
on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'roles/common/**'
      - '.github/workflows/_template-molecule-test.yaml'
      - '.github/workflows/molecule-role-common.yaml'
  push:
    paths:
      - 'roles/common/**'
      - '.github/workflows/_template-molecule-test.yaml'
      - '.github/workflows/molecule-role-common.yaml'

jobs:
  integration:
    name: "Molecule"
    uses: ./.github/workflows/_template-molecule-test.yaml
    with:
      role: common
